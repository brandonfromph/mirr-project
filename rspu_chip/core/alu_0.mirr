import "memory/mem_0.mirr" as mem;

def alu_core_0(clk: signal in bool, op1: signal in u16, op2: signal out u16) {
    reflect {
        mem_block_0(${clk}, ${op1}, ${op2});
        guard alu_active {
            when ${clk}
            for 1 cycles;
        }
        signal stage_0: internal bool;
        signal stage_1: internal bool;
        signal stage_2: internal bool;
        signal stage_3: internal bool;
        signal stage_4: internal bool;
        signal stage_5: internal bool;
        signal stage_6: internal bool;
        signal stage_7: internal bool;
        signal stage_8: internal bool;
        signal stage_9: internal bool;
        signal stage_10: internal bool;
        signal stage_11: internal bool;
        signal stage_12: internal bool;
        signal stage_13: internal bool;
        signal stage_14: internal bool;
        signal stage_15: internal bool;
    }
}
def alu_bulk_0_0(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_1(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_2(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_3(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_4(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_5(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_6(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_7(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_8(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}
def alu_bulk_0_9(x: signal in bool, y: signal out bool) {
    reflect { reflex pass { on ${x} { ${y} = true; } } }
}

module core_subsystem_0 {}
