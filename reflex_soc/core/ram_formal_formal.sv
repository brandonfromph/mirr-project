// Auto-generated by MIRR compiler (Phase 6)
// Do not edit — regenerate from .mirr source.
// Target: SystemVerilog (.sv)

/* verilator lint_off UNOPTFLAT */
/* verilator lint_off DECLFILENAME */
/* verilator lint_off WIDTH */
/* verilator lint_off WIDTHTRUNC */
/* verilator lint_off WIDTHEXPAND */
/* verilator lint_off SYNCASYNCNET */
/* verilator lint_off PINCONNECTEMPTY */
/* verilator lint_off UNDRIVEN */
/* verilator lint_off UNUSED */

module ram_formal (
  input  logic        clk,
  input  logic        rst_n,
  output logic [63:0] dout
);

  // Internal signals
  // source: <unknown>:14
  logic [13:0] addr;
  // source: <unknown>:15
  logic [63:0] din;
  // source: <unknown>:21
  logic [13:0] addr_d1;
  // source: <unknown>:22
  logic [63:0] din_d1;
  // source: <unknown>:23
  logic [13:0] addr_d2;
  // source: <unknown>:24
  logic [63:0] din_d2;
  // source: <unknown>:25
  logic        valid_d1;
  // source: <unknown>:26
  logic        valid_d2;

  // Physical Power-On Reset Initialization (Matches FPGA target reality)
  initial begin
    addr = '0;
    din = '0;
    addr_d1 = '0;
    din_d1 = '0;
    addr_d2 = '0;
    din_d2 = '0;
    valid_d1 = '0;
    valid_d2 = '0;
  end

  // ── Input Synchronizer Chains ──

  // Temporal signals
  logic        always_out;

  // ── Temporal Guards ──


  // Guard: always (len 6, bytes [97, 108, 119, 97, 121, 115]) — always for 0 cycle (combinational)
  logic always_cond;
  assign always_cond = 1'b1;
  assign always_out = always_cond;

  // ── Reflex Assignments ──

  // ── Reflex Signal Drivers ──

  // Unified Reflex Block for: addr_d1 (@clk)
  initial begin
    addr_d1 = '0;
  end
  always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      addr_d1 <= '0;
    end else begin
  // source: <unknown>:28
      if (always_out) addr_d1 <= addr;
    end
  end

  // Unified Reflex Block for: addr_d2 (@clk)
  initial begin
    addr_d2 = '0;
  end
  always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      addr_d2 <= '0;
    end else begin
  // source: <unknown>:28
      if (always_out) addr_d2 <= addr_d1;
    end
  end

  // Unified Reflex Block for: din_d1 (@clk)
  initial begin
    din_d1 = '0;
  end
  always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      din_d1 <= '0;
    end else begin
  // source: <unknown>:28
      if (always_out) din_d1 <= din;
    end
  end

  // Unified Reflex Block for: din_d2 (@clk)
  initial begin
    din_d2 = '0;
  end
  always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      din_d2 <= '0;
    end else begin
  // source: <unknown>:28
      if (always_out) din_d2 <= din_d1;
    end
  end

  // Unified Reflex Block for: valid_d1 (@clk)
  initial begin
    valid_d1 = '0;
  end
  always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      valid_d1 <= '0;
    end else begin
  // source: <unknown>:28
      if (always_out) valid_d1 <= 1'b1;
    end
  end

  // Unified Reflex Block for: valid_d2 (@clk)
  initial begin
    valid_d2 = '0;
  end
  always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
      valid_d2 <= '0;
    end else begin
  // source: <unknown>:28
      if (always_out) valid_d2 <= valid_d1;
    end
  end

  // ── Structural Module Instantiations ──

  ram ram_inst_8 (
    .clk(clk),
    .addr(addr),
    .din(din),
    .dout(dout)
  );

  // ── Safety Properties (SVA) ──

  // Enforce reset sequence for formal verification
  reg f_past_valid = 0;
  always_ff @(posedge clk) begin
    f_past_valid <= 1;
  end
  always_comb begin
    if (!f_past_valid) assume (!rst_n);
  end

  // source: <unknown>:41
  // property: read_after_write
  always @(posedge clk) begin
    if (rst_n) begin
      read_after_write: assert((!((valid_d2 & (addr_d2 == addr_d1)))) || ((dout == din_d2)));
    end
  end

endmodule
