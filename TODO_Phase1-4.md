## Active TODO

- [ ] Adopt Rust-native SQLite path for control-plane persistence (replace any remaining prototype TS-side persistence behavior with Rust-owned storage wiring and tests).
- [ ] Audit `crates/mirr-arsenal-wasm` scope and ownership (document purpose, current consumers, and whether it stays as a standalone WASM bridge crate or folds into another surface).

