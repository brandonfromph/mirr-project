def pat_09(s: signal out bool) {
    reflect {
        reflex r {
            on always {
                ${s} = true;
            }
        }
    }
}
module inter_mod_09 {
    signal s_out: out bool;
    pat_09(s_out);
}
