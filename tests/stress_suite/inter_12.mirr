def pat_12(s: signal out bool) {
    reflect {
        reflex r {
            on always {
                ${s} = true;
            }
        }
    }
}
module inter_mod_12 {
    signal s_out: out bool;
    pat_12(s_out);
}
