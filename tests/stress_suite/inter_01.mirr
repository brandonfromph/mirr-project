def pat_01(s: signal out bool) {
    reflect {
        reflex r {
            on always {
                ${s} = true;
            }
        }
    }
}
module inter_mod_01 {
    signal s_out: out bool;
    pat_01(s_out);
}
