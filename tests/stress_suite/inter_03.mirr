def pat_03(s: signal out bool) {
    reflect {
        reflex r {
            on always {
                ${s} = true;
            }
        }
    }
}
module inter_mod_03 {
    signal s_out: out bool;
    pat_03(s_out);
}
