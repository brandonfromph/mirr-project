def pat_06(s: signal out bool) {
    reflect {
        reflex r {
            on always {
                ${s} = true;
            }
        }
    }
}
module inter_mod_06 {
    signal s_out: out bool;
    pat_06(s_out);
}
