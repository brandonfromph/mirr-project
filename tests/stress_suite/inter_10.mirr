def pat_10(s: signal out bool) {
    reflect {
        reflex r {
            on always {
                ${s} = true;
            }
        }
    }
}
module inter_mod_10 {
    signal s_out: out bool;
    pat_10(s_out);
}
